{
	"$schema": "https://docs.renovatebot.com/renovate-schema.json",
	"description": "Node.js project configuration for Renovate. Bun versions cannot be updated through package.json packageManager because Corepack and Renovate do not support Bun there; use .bun-version with package-managers/bun instead.",
	"packageRules": [
		{
			"description": "Wait for npm releases to mature before raising update branches",
			"matchDatasources": ["npm"],
			"minimumReleaseAge": "1 day",
			"internalChecksFilter": "strict"
		},
		{
			"description": "Wait longer for Node.js runtime releases than for npm packages. What has to catch up here is other vendors' build images (Cloudflare Pages and node-build definitions, Docker official images, actions/setup-node manifests), not the package itself, and they lag the release by days. Raising the branch on release day makes builds fail with errors such as 'node-build: definition not found'.",
			"matchDatasources": ["node-version"],
			"minimumReleaseAge": "5 days",
			"internalChecksFilter": "strict"
		},
		{
			"matchDatasources": ["npm"],
			"groupName": "Node.js dependencies",
			"semanticCommitType": "chore",
			"semanticCommitScope": "deps-node"
		},
		{
			"matchDatasources": ["npm"],
			"matchUpdateTypes": ["major"],
			"groupName": "Node.js major updates"
		},
		{
			"matchDatasources": ["npm"],
			"matchUpdateTypes": ["minor", "patch"],
			"groupName": "Node.js minor and patch updates"
		},
		{
			"matchDatasources": ["npm"],
			"matchDepTypes": ["devDependencies"],
			"groupName": "Node.js dev dependencies"
		},
		{
			"matchPackageNames": ["@types/**"],
			"groupName": "Type definitions"
		},
		{
			"description": "Do not require minimum release age for package-manager controlled lockfile maintenance",
			"matchDatasources": ["npm"],
			"matchUpdateTypes": ["lockFileMaintenance"],
			"minimumReleaseAge": null,
			"prBodyNotes": [
				"Renovate lock file maintenance delegates updates to the package manager, so confirm the package manager enforces its own minimum release age policy."
			]
		},
		{
			"description": "Do not require minimum release age for package replacements",
			"matchDatasources": ["npm"],
			"matchUpdateTypes": ["replacement"],
			"minimumReleaseAge": null,
			"prBodyNotes": [
				"Renovate replacement updates do not reliably carry release age metadata, so manually confirm the package age before merging."
			]
		},
		{
			"description": "Do not require minimum release age for package pinning",
			"matchDatasources": ["npm"],
			"matchUpdateTypes": ["pin"],
			"minimumReleaseAge": null,
			"prBodyNotes": [
				"Renovate pin updates do not reliably carry release age metadata, so manually confirm the package age before merging."
			]
		}
	],
	"lockFileMaintenance": {
		"enabled": true
	}
}
